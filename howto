1) a XOR 0 = a
2) a XOR a = 0
3) a XOR b = b XOR a
4) (a XOR b) XOR b = a

using 4;


usage           make;
		cryptor S1.txt S2.txt
		//input key
		cryptor S2.txt S3.txt
		//input key


		S1.txt =  S3.txt  if key is valid
